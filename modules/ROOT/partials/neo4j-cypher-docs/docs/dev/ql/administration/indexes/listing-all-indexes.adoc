[[administration-indexes-listing-all-indexes]]
== Listing all indexes ==

To list all indexes with the default output columns, the `SHOW INDEXES` command can be used.
If all columns are required, use `SHOW INDEXES YIELD *`.

.Query
include::includes/administration-indexes-listing-all-indexes.query.adoc[]


One of the output columns from `SHOW INDEXES` is the name of the index.
This can be used to drop the index with the <<administration-indexes-drop-an-index, `DROP INDEX` command>>.

.Result
include::includes/administration-indexes-listing-all-indexes.result.adoc[]

.Try this query live
include::includes/administration-indexes-listing-all-indexes.console.adoc[]

